# @package apps.todo
# Structure only -- colors/fonts come from the shared theme. Select with
# `+apps/todo/layout=kanban_board` (or add `layout: default` to apps/todo/default.yaml defaults).
layout: kanban_board
